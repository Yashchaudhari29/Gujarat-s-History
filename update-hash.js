const fs = require('fs'); const crypto = require('crypto'); const styles = fs.readFileSync('src/styles.css', 'utf8'); const hash = crypto.createHash('sha256').update(styles.trim()).digest('hex'); const baseline = JSON.parse(fs.readFileSync('scripts/migration-baseline.json', 'utf8')); baseline.styles = hash; fs.writeFileSync('scripts/migration-baseline.json', JSON.stringify(baseline, null, 2), 'utf8');
